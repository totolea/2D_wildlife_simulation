# EcoSim — Prédateurs/Proies évolutifs (2D)

- Lancement :
  pip install -r requirements.txt
  python ecosim_sprites_gui.py

- Contrôles : SPACE (pause), R (reset), clic gauche (food), clic droit (prédateur), molette (± proie).
